mod helloworld;
mod random_num;
mod ownership;
mod structure;
mod panic;


fn main() {

    // This is how u import a function from different file. mod filename(prolly)
    // helloworld::main();
    // random_num::main();
    // ownership::ownership_fn();
    structure::main();
    // panic::main();
}
